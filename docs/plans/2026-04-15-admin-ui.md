# Phase 3 — 어드민 UI 구현 플랜

**생성일**: 2026-04-15  
**상태**: 구현 중

---

## Task 1: GET /admin/auth/me 엔드포인트 추가 (백엔드)

**파일**: `backend/src/main/java/com/esports/domain/auth/AdminAuthController.java`  
**변경**: `@GetMapping("/me")` 메서드 추가. 반환 `ApiResponse<Void>`. SecurityConfig의 `/admin/**` 인증 규칙으로 미인증 시 401 자동 반환.  
**테스트**: `backend/src/test/java/com/esports/domain/auth/AdminAuthControllerTest.java` — `getMeWithValidTokenReturns200()`, `getMeWithoutTokenReturns401()` 추가  
**의존**: 없음

---

## Task 2: 프론트엔드 어드민 폼 타입 정의

**파일**: `frontend/src/types/adminForms.ts` (신규)  
**변경**: zod 스키마 5종 + infer 타입 export
- `matchCreateSchema` / `MatchCreateFormValues`
- `matchUpdateSchema` / `MatchUpdateFormValues`
- `matchResultSchema` / `MatchResultFormValues`
- `teamFormSchema` / `TeamFormValues`
- `playerFormSchema` / `PlayerFormValues`

---

## Task 3: 어드민 인증 API 함수

**파일**: `frontend/src/api/admin/auth.ts` (신규)  
**변경**:
- `pingAdminAuth(): Promise<void>` — `GET /admin/auth/me`
- `logoutAdmin(): Promise<void>` — `POST /admin/auth/logout`

---

## Task 4: 어드민 경기 API 함수

**파일**: `frontend/src/api/admin/adminMatches.ts` (신규)  
**변경**:
- `adminFetchMatches(page?)` — `GET /api/v1/matches`
- `adminCreateMatch(data)` — `POST /admin/matches`
- `adminUpdateMatch(id, data)` — `PUT /admin/matches/{id}`
- `adminDeleteMatch(id)` — `DELETE /admin/matches/{id}`
- `adminCreateMatchResult(matchId, data)` — `POST /admin/matches/{matchId}/result`
- `adminUpdateMatchResult(matchId, data)` — `PUT /admin/matches/{matchId}/result`

---

## Task 5: 어드민 팀 API 함수

**파일**: `frontend/src/api/admin/adminTeams.ts` (신규)  
**변경**:
- `adminCreateTeam(data)` — `POST /admin/teams`
- `adminUpdateTeam(id, data)` — `PUT /admin/teams/{id}`
- `adminDeleteTeam(id)` — `DELETE /admin/teams/{id}`

---

## Task 6: 어드민 선수 API 함수

**파일**: `frontend/src/api/admin/adminPlayers.ts` (신규)  
**변경**:
- `adminCreatePlayer(data)` — `POST /admin/players`
- `adminUpdatePlayer(id, data)` — `PUT /admin/players/{id}`
- `adminDeletePlayer(id)` — `DELETE /admin/players/{id}`

---

## Task 7: useAdminAuth 훅

**파일**: `frontend/src/hooks/admin/useAdminAuth.ts` (신규)  
**변경**: React Query로 `pingAdminAuth` 호출. `retry: false`, `staleTime: 5분`. `logout()` 함수 포함 (queryClient.clear + navigate)

---

## Task 8: useAdminMatches 훅

**파일**: `frontend/src/hooks/admin/useAdminMatches.ts` (신규)  
**변경**: `useAdminMatchList`, `useAdminCreateMatch`, `useAdminUpdateMatch`, `useAdminDeleteMatch`, `useAdminCreateMatchResult`, `useAdminUpdateMatchResult`

---

## Task 9: useAdminTeams 훅

**파일**: `frontend/src/hooks/admin/useAdminTeams.ts` (신규)  
**변경**: `useAdminTeamList`, `useAdminCreateTeam`, `useAdminUpdateTeam`, `useAdminDeleteTeam`

---

## Task 10: useAdminPlayers 훅

**파일**: `frontend/src/hooks/admin/useAdminPlayers.ts` (신규)  
**변경**: `useAdminPlayerDetail`, `useAdminCreatePlayer`, `useAdminUpdatePlayer`, `useAdminDeletePlayer`

---

## Task 11: AdminRoute (ProtectedRoute)

**파일**: `frontend/src/components/admin/AdminRoute.tsx` (신규)  
**변경**: `useAdminAuth()` → 로딩 시 스피너, isError 시 `<Navigate to="/admin/login" replace />`, 정상 시 `<Outlet />`

---

## Task 12: AdminSidebar

**파일**: `frontend/src/components/admin/layout/AdminSidebar.tsx` (신규)  
**변경**: NavLink 4개 (대시보드/경기/팀/선수). active 강조. `w-56 h-screen bg-gray-900 text-white`

---

## Task 13: AdminTopBar

**파일**: `frontend/src/components/admin/layout/AdminTopBar.tsx` (신규)  
**변경**: "E-sports Admin" 텍스트 + 로그아웃 버튼. props: `onLogout: () => void`

---

## Task 14: AdminLayout

**파일**: `frontend/src/components/admin/layout/AdminLayout.tsx` (신규)  
**변경**: `AdminSidebar` + `AdminTopBar` + `<Outlet />`. `useAdminAuth()`에서 logout 가져옴

---

## Task 15: AdminConfirmDialog

**파일**: `frontend/src/components/admin/shared/AdminConfirmDialog.tsx` (신규)  
**변경**: shadcn Dialog 기반 삭제 확인 다이얼로그. props: `open`, `title`, `description`, `onConfirm`, `onCancel`, `isLoading`

---

## Task 16: AdminStatusBadge

**파일**: `frontend/src/components/admin/shared/AdminStatusBadge.tsx` (신규)  
**변경**: MatchStatus별 배지. SCHEDULED→예정, ONGOING→진행중, COMPLETED→완료, CANCELLED→취소

---

## Task 17: AdminLoginPage

**파일**: `frontend/src/pages/admin/AdminLoginPage.tsx` (신규)  
**변경**: react-hook-form + zod. `POST /admin/auth/login`. 성공 시 `/admin/dashboard` navigate. 실패 시 에러 메시지

---

## Task 18: AdminDashboardPage

**파일**: `frontend/src/pages/admin/AdminDashboardPage.tsx` (신규)  
**변경**: 경기/팀/선수 카운트 카드 3개. 각 관리 페이지 링크

---

## Task 19: AdminMatchListPage

**파일**: `frontend/src/pages/admin/matches/AdminMatchListPage.tsx` (신규)  
**변경**: 경기 목록 테이블. 수정/삭제/결과 액션. 페이지네이션. 삭제 시 AdminConfirmDialog

---

## Task 20: AdminMatchFormPage (신규/수정 공용)

**파일**: `frontend/src/pages/admin/matches/AdminMatchFormPage.tsx` (신규)  
**변경**: `useParams`로 id 유무 판별. 수정 모드: `GET /api/v1/matches/{id}`로 기존값 로드 후 reset. 필드: 종목ID, 팀A/B ID, 대회명, 단계, 예정시각, 상태(수정 모드만)

---

## Task 21: AdminMatchResultPage

**파일**: `frontend/src/pages/admin/matches/AdminMatchResultPage.tsx` (신규)  
**변경**: matchId 기반. 필드: 승리팀ID, A/B 점수, 경기시각, VOD URL, 비고

---

## Task 22: AdminTeamListPage

**파일**: `frontend/src/pages/admin/teams/AdminTeamListPage.tsx` (신규)  
**변경**: 팀 목록 테이블. 수정/삭제 액션

---

## Task 23: AdminTeamFormPage (신규/수정 공용)

**파일**: `frontend/src/pages/admin/teams/AdminTeamFormPage.tsx` (신규)  
**변경**: 수정 모드: `fetchTeamById`로 기존값 로드. 필드: 팀명, 약칭, 지역, 로고URL, 종목ID, primaryColor(text "#RRGGBB"), secondaryColor

---

## Task 24: AdminPlayerListPage

**파일**: `frontend/src/pages/admin/players/AdminPlayerListPage.tsx` (신규)  
**변경**: `useAdminTeamList` 전체 팀 → players flat map. 수정/삭제 액션

---

## Task 25: AdminPlayerFormPage (신규/수정 공용)

**파일**: `frontend/src/pages/admin/players/AdminPlayerFormPage.tsx` (신규)  
**변경**: 수정 모드: `fetchPlayerById`로 기존값 로드. 필드: 닉네임, 실명, 역할, 국적, 프로필이미지URL, 팀ID

---

## Task 26: App.tsx 어드민 라우팅 연결

**파일**: `frontend/src/App.tsx`  
**변경**: `/admin/login` 비보호 + `/admin/*` AdminRoute > AdminLayout 구조 추가

---

## Task 27: 백엔드 빌드 검증

**명령어**: `cd backend && ./gradlew test --no-daemon`  
**의존**: Task 1

---

## Task 28: 프론트엔드 빌드 검증

**명령어**: `cd frontend && npm run build`  
**의존**: Task 26
